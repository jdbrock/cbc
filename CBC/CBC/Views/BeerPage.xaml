<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Xamarin.Forms.Labs.Controls;assembly=XLabs.Forms"
             x:Class="CBC.BeerPage">

    <ContentPage.ToolbarItems>
        <ToolbarItem Name="A-Z" Order="Secondary" Priority="0" Command="{Binding OrderAZCommand}" />
        <ToolbarItem Name="Fav" Order="Secondary" Priority="0" Command="{Binding OrderFavCommand}" />
    </ContentPage.ToolbarItems>

    <ListView ItemsSource="{Binding Beers}" RowHeight="76" ItemTapped="OnBeerTapped" ItemSelected="OnBeerSelected" Refreshing="OnRefreshing" IsPullToRefreshEnabled="true" GroupDisplayBinding="{Binding StyleName}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <!--<TextCell Text="{Binding BreweryAndBeerName}" />-->
                <ViewCell>
                    <ViewCell.ContextActions>
                        <MenuItem Clicked="OnBeerFavorite" Text="Favorite" IsDestructive="False" />
                    </ViewCell.ContextActions>
                    <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" Padding="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackLayout Grid.Row="0" Grid.Column="0" Grid.RowSpan="3" Padding="12" VerticalOptions="Center">
                            <Image Source="{Binding MetaData.StatusImageUri}" />
                        </StackLayout>

                        <Label Grid.Row="0" Grid.Column="1" Text="{Binding BreweryName}"  LineBreakMode="TailTruncation" FontSize="Medium" FontAttributes="Bold" />
                        <Label Grid.Row="1" Grid.Column="1" Text="{Binding BeerName}"     LineBreakMode="TailTruncation" FontSize="Small" />
                        <Label Grid.Row="2" Grid.Column="1" Text="{Binding ABVAndStyle}"  LineBreakMode="TailTruncation" FontSize="Micro" Opacity="0.5" />

                        <!--<Label Grid.Row="0" Grid.Column="2" Text="{Binding DateBottled,  StringFormat='{0:yyyy-MM-dd}'}" FontSize="Micro" Opacity="0.5" />
                        <Label Grid.Row="1" Grid.Column="2" Text="{Binding DateAdded,    StringFormat='{0:yyyy-MM-dd}'}" FontSize="Micro" Opacity="0.5" />
                        <Label Grid.Row="2" Grid.Column="2" Text="{Binding DateExpiry,   StringFormat='{0:yyyy-MM-dd}'}" FontSize="Micro" Opacity="0.5" />-->
                    </Grid>

                    <!--<StackLayout HorizontalOptions="StartAndExpand" Orientation="Horizontal">
                      <StackLayout Padding="5,0,0,0" VerticalOptions="StartAndExpand" Orientation="Vertical">
                        <Label Text="{Binding .}" YAlign="Center" FontSize="Medium" />
                        <Label Text="hint: trigger a context action" YAlign="Center" FontSize="Small" />
                      </StackLayout>
                    </StackLayout>-->
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
    
</ContentPage>